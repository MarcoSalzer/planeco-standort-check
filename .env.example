DATABASE_URL=

BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=
DRY_RUN_EMAIL=
MAX_EMAILS_PER_DAY=

ADMIN_USER=
ADMIN_PASSWORD_HASH=
SESSION_SECRET=
RETRY_SECRET=
EDIT_TOKEN_SECRET=

# Leer oder "alle" = alle 16 Bundesländer. Sonst kommagetrennte Liste
# echter Bundesland-Namen (z.B. "Bayern,Hessen") - ein unbekannter Name
# lässt die Anwendung beim Start mit einer Fehlermeldung abbrechen statt
# still einen Filter zu bilden, der auf nichts passt.
SERVICE_AREA_STATES=
# Minuten bis Geocoding & Co. nach einem Submit verarbeitet werden
# (Korrekturfenster, Konzept §G). Für eine Demo kurzzeitig auf einen
# kleinen Wert (z.B. 1) senkbar.
PROCESS_DELAY_MINUTES=
NOMINATIM_USER_AGENT=
DRY_RUN_GEOCODE=
MAX_GEOCODE_PER_MINUTE=
# Wie viele Leads EIN Aufruf von POST /admin/retry maximal geokodiert
# (getrennt von MAX_GEOCODE_PER_MINUTE, s. app/config.py). Default 5.
GEOCODE_BATCH_SIZE=

KONTAKT_EMAIL=
KONTAKT_TELEFON=
